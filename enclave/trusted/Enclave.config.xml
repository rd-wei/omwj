<EnclaveConfiguration>
  <ProdID>0</ProdID>
  <ISVSVN>0</ISVSVN>
  <StackMaxSize>0x40000</StackMaxSize>   <!-- 256 KB -->
  <!-- 8 GB static heap.  Physical EPC is ~378 MB, so large runs rely on
       kernel EPC paging; the kernel (5.15) predates EDMM, so the heap must
       be committed statically at load. -->
  <HeapMaxSize>0x200000000</HeapMaxSize>
  <TCSNum>1</TCSNum>
  <TCSPolicy>1</TCSPolicy>
  <DisableDebug>0</DisableDebug>
  <MiscSelect>0</MiscSelect>
  <MiscMask>0xFFFFFFFF</MiscMask>
</EnclaveConfiguration>
